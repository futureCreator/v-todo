# 위시리스트 기능 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v-todo에 위시리스트(물건/경험) 기능을 추가하여 사고 싶은 것과 하고 싶은 것을 카드형 UI로 관리할 수 있게 한다.

**Architecture:** 기존 schedule 기능과 동일한 패턴(JSON 파일 저장소 → API 라우트 → React 클라이언트)을 따른다. `types/index.ts`에 타입 추가, `lib/wish-store.ts`로 저장소, `/api/wishes` API 라우트, 3개의 UI 컴포넌트(WishlistView, WishItem, AddWishSheet), 기존 page.tsx와 BottomNav에 탭 연결.

**Tech Stack:** Next.js 16, React 19, TypeScript, Tailwind CSS 4, uuid, Vitest

---

## 파일 구조

- **Create:** `types/index.ts` (수정 — WishItem 등 타입 추가)
- **Create:** `lib/wish-store.ts` (JSON 파일 저장소)
- **Create:** `lib/__tests__/wish-store.test.ts` (저장소 테스트)
- **Create:** `app/api/wishes/route.ts` (GET, POST)
- **Create:** `app/api/wishes/[id]/route.ts` (PUT, DELETE)
- **Create:** `lib/__tests__/api-wishes.test.ts` (API 테스트)
- **Create:** `components/WishItem.tsx` (카드 컴포넌트)
- **Create:** `components/AddWishSheet.tsx` (추가/수정 바텀 시트)
- **Create:** `components/WishlistView.tsx` (목록 뷰)
- **Modify:** `app/page.tsx` (탭 순서, 위시리스트 연결)
- **Modify:** `components/BottomNav.tsx` (탭 순서, 아이콘 추가)

---

### Task 1: 타입 정의

**Files:**
- Modify: `types/index.ts`

- [ ] **Step 1: WishItem 타입 및 관련 타입을 types/index.ts에 추가**

`types/index.ts` 파일 끝에 다음을 추가한다:

```typescript
// Wishes
export type WishCategory = "item" | "experience";

export interface WishItem {
  id: string;
  title: string;
  category: WishCategory;
  price: number | null;
  url: string | null;
  imageUrl: string | null;
  memo: string | null;
  completed: boolean;
  completedAt: string | null;
  createdAt: string;
}

export interface WishStore {
  wishes: WishItem[];
}

export interface CreateWishRequest {
  title: string;
  category: WishCategory;
  price?: number | null;
  url?: string | null;
  imageUrl?: string | null;
  memo?: string | null;
}

export interface UpdateWishRequest {
  title?: string;
  category?: WishCategory;
  price?: number | null;
  url?: string | null;
  imageUrl?: string | null;
  memo?: string | null;
  completed?: boolean;
}

export const VALID_WISH_CATEGORIES: WishCategory[] = ["item", "experience"];
```

또한 `Section` 타입을 수정한다:

```typescript
// 기존
export type Section = "todo" | "note" | "dday";
// 변경
export type Section = "todo" | "note" | "wish" | "dday";
```

- [ ] **Step 2: 커밋**

```bash
git add types/index.ts
git commit -m "feat(wish): add WishItem types and update Section type"
```

---

### Task 2: 위시 저장소

**Files:**
- Create: `lib/wish-store.ts`
- Test: `lib/__tests__/wish-store.test.ts`

- [ ] **Step 1: wish-store 테스트 작성**

`lib/__tests__/wish-store.test.ts`:

```typescript
import { describe, it, expect, beforeEach, afterEach } from "vitest";
import fs from "fs/promises";
import path from "path";
import { readWishes, writeWishes, WISH_PATH } from "../wish-store";
import { DATA_DIR } from "../store";
import type { WishItem } from "@/types";

describe("wish-store", () => {
  beforeEach(async () => {
    await fs.mkdir(DATA_DIR, { recursive: true });
    await fs.writeFile(WISH_PATH, JSON.stringify({ wishes: [] }));
  });

  afterEach(async () => {
    try { await fs.unlink(WISH_PATH); } catch {}
    try { await fs.unlink(path.join(DATA_DIR, "wishes.tmp.json")); } catch {}
  });

  it("reads empty wishes", async () => {
    const wishes = await readWishes();
    expect(wishes).toEqual([]);
  });

  it("writes and reads wishes", async () => {
    const wish: WishItem = {
      id: "wish-1",
      title: "맥북 프로",
      category: "item",
      price: 3000000,
      url: "https://apple.com",
      imageUrl: null,
      memo: "M4 Max",
      completed: false,
      completedAt: null,
      createdAt: "2026-04-02T00:00:00Z",
    };
    await writeWishes([wish]);
    const result = await readWishes();
    expect(result).toHaveLength(1);
    expect(result[0].title).toBe("맥북 프로");
    expect(result[0].price).toBe(3000000);
  });

  it("creates file if not exists", async () => {
    await fs.unlink(WISH_PATH);
    const wishes = await readWishes();
    expect(wishes).toEqual([]);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npx vitest run lib/__tests__/wish-store.test.ts`
Expected: FAIL — `wish-store` 모듈이 존재하지 않음

- [ ] **Step 3: wish-store 구현**

`lib/wish-store.ts`:

```typescript
import fs from "fs/promises";
import path from "path";
import type { WishItem, WishStore } from "@/types";

const DATA_DIR = process.env.DATA_DIR || path.join(process.cwd(), "data");
export const WISH_PATH = path.join(DATA_DIR, "wishes.json");
const TMP_PATH = path.join(DATA_DIR, "wishes.tmp.json");

export async function readWishes(): Promise<WishItem[]> {
  try {
    const raw = await fs.readFile(WISH_PATH, "utf-8");
    const parsed: WishStore = JSON.parse(raw);
    return parsed.wishes;
  } catch (err: unknown) {
    if (
      err instanceof Error &&
      "code" in err &&
      (err as NodeJS.ErrnoException).code === "ENOENT"
    ) {
      await fs.mkdir(DATA_DIR, { recursive: true });
      await fs.writeFile(WISH_PATH, JSON.stringify({ wishes: [] }));
      return [];
    }
    console.error("Failed to parse wishes.json, resetting:", err);
    await fs.writeFile(WISH_PATH, JSON.stringify({ wishes: [] }));
    return [];
  }
}

export async function writeWishes(wishes: WishItem[]): Promise<void> {
  const data: WishStore = { wishes };
  await fs.mkdir(DATA_DIR, { recursive: true });
  await fs.writeFile(TMP_PATH, JSON.stringify(data, null, 2));
  await fs.rename(TMP_PATH, WISH_PATH);
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run lib/__tests__/wish-store.test.ts`
Expected: 3 tests PASS

- [ ] **Step 5: 커밋**

```bash
git add lib/wish-store.ts lib/__tests__/wish-store.test.ts
git commit -m "feat(wish): add wish-store with JSON file persistence"
```

---

### Task 3: 위시 API 라우트 (GET, POST)

**Files:**
- Create: `app/api/wishes/route.ts`
- Test: `lib/__tests__/api-wishes.test.ts`

- [ ] **Step 1: API 테스트 작성 (GET, POST)**

`lib/__tests__/api-wishes.test.ts`:

```typescript
import { describe, it, expect, beforeEach } from "vitest";
import fs from "fs/promises";
import { WISH_PATH } from "../wish-store";
import { DATA_DIR } from "../store";

import { GET, POST } from "@/app/api/wishes/route";

describe("GET /api/wishes", () => {
  beforeEach(async () => {
    await fs.mkdir(DATA_DIR, { recursive: true });
    await fs.writeFile(WISH_PATH, JSON.stringify({ wishes: [] }));
  });

  it("returns empty array initially", async () => {
    const res = await GET();
    const body = await res.json();
    expect(res.status).toBe(200);
    expect(body.data).toEqual([]);
  });
});

describe("POST /api/wishes", () => {
  beforeEach(async () => {
    await fs.mkdir(DATA_DIR, { recursive: true });
    await fs.writeFile(WISH_PATH, JSON.stringify({ wishes: [] }));
  });

  it("creates an item wish", async () => {
    const req = new Request("http://localhost/api/wishes", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        title: "맥북 프로",
        category: "item",
        price: 3000000,
        url: "https://apple.com",
      }),
    });
    const res = await POST(req);
    const body = await res.json();
    expect(res.status).toBe(201);
    expect(body.data.title).toBe("맥북 프로");
    expect(body.data.category).toBe("item");
    expect(body.data.price).toBe(3000000);
    expect(body.data.id).toBeDefined();
    expect(body.data.completed).toBe(false);
  });

  it("creates an experience wish with minimal fields", async () => {
    const req = new Request("http://localhost/api/wishes", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        title: "제주도 여행",
        category: "experience",
      }),
    });
    const res = await POST(req);
    const body = await res.json();
    expect(res.status).toBe(201);
    expect(body.data.category).toBe("experience");
    expect(body.data.price).toBeNull();
    expect(body.data.url).toBeNull();
  });

  it("rejects empty title", async () => {
    const req = new Request("http://localhost/api/wishes", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ title: "", category: "item" }),
    });
    const res = await POST(req);
    expect(res.status).toBe(400);
  });

  it("rejects invalid category", async () => {
    const req = new Request("http://localhost/api/wishes", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ title: "test", category: "invalid" }),
    });
    const res = await POST(req);
    expect(res.status).toBe(400);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npx vitest run lib/__tests__/api-wishes.test.ts`
Expected: FAIL — route 모듈이 존재하지 않음

- [ ] **Step 3: GET, POST 라우트 구현**

`app/api/wishes/route.ts`:

```typescript
import { NextResponse } from "next/server";
import { v4 as uuidv4 } from "uuid";
import { readWishes, writeWishes } from "@/lib/wish-store";
import type { CreateWishRequest, WishItem, ApiResponse } from "@/types";

const VALID_CATEGORIES = ["item", "experience"];

export async function GET(): Promise<NextResponse<ApiResponse<WishItem[]>>> {
  try {
    const wishes = await readWishes();
    return NextResponse.json({ data: wishes });
  } catch {
    return NextResponse.json({ error: "서버 오류가 발생했습니다." }, { status: 500 });
  }
}

export async function POST(
  request: Request
): Promise<NextResponse<ApiResponse<WishItem>>> {
  try {
    const body: CreateWishRequest = await request.json();

    if (!body.title || typeof body.title !== "string" || body.title.trim().length === 0 || body.title.length > 200) {
      return NextResponse.json({ error: "제목은 1~200자여야 합니다." }, { status: 400 });
    }
    if (!VALID_CATEGORIES.includes(body.category)) {
      return NextResponse.json({ error: "올바른 카테고리를 선택해주세요." }, { status: 400 });
    }

    const wish: WishItem = {
      id: uuidv4(),
      title: body.title.trim(),
      category: body.category,
      price: body.price ?? null,
      url: body.url ?? null,
      imageUrl: body.imageUrl ?? null,
      memo: body.memo ?? null,
      completed: false,
      completedAt: null,
      createdAt: new Date().toISOString(),
    };

    const wishes = await readWishes();
    wishes.push(wish);
    await writeWishes(wishes);

    return NextResponse.json({ data: wish }, { status: 201 });
  } catch {
    return NextResponse.json({ error: "서버 오류가 발생했습니다." }, { status: 500 });
  }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run lib/__tests__/api-wishes.test.ts`
Expected: 5 tests PASS

- [ ] **Step 5: 커밋**

```bash
git add app/api/wishes/route.ts lib/__tests__/api-wishes.test.ts
git commit -m "feat(wish): add GET/POST /api/wishes routes"
```

---

### Task 4: 위시 API 라우트 (PUT, DELETE)

**Files:**
- Create: `app/api/wishes/[id]/route.ts`
- Modify: `lib/__tests__/api-wishes.test.ts`

- [ ] **Step 1: PUT, DELETE 테스트 추가**

`lib/__tests__/api-wishes.test.ts` 파일 끝에 다음을 추가:

```typescript
import { PUT, DELETE } from "@/app/api/wishes/[id]/route";
import type { WishItem } from "@/types";

describe("PUT /api/wishes/[id]", () => {
  beforeEach(async () => {
    await fs.mkdir(DATA_DIR, { recursive: true });
    const wish: WishItem = {
      id: "wish-1",
      title: "맥북 프로",
      category: "item",
      price: 3000000,
      url: null,
      imageUrl: null,
      memo: null,
      completed: false,
      completedAt: null,
      createdAt: "2026-04-02T00:00:00Z",
    };
    await fs.writeFile(WISH_PATH, JSON.stringify({ wishes: [wish] }));
  });

  it("updates title", async () => {
    const req = new Request("http://localhost/api/wishes/wish-1", {
      method: "PUT",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ title: "맥북 에어" }),
    });
    const res = await PUT(req, { params: Promise.resolve({ id: "wish-1" }) });
    const body = await res.json();
    expect(res.status).toBe(200);
    expect(body.data.title).toBe("맥북 에어");
  });

  it("toggles completed", async () => {
    const req = new Request("http://localhost/api/wishes/wish-1", {
      method: "PUT",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ completed: true }),
    });
    const res = await PUT(req, { params: Promise.resolve({ id: "wish-1" }) });
    const body = await res.json();
    expect(body.data.completed).toBe(true);
    expect(body.data.completedAt).toBeDefined();
  });

  it("returns 404 for unknown id", async () => {
    const req = new Request("http://localhost/api/wishes/nope", {
      method: "PUT",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ title: "x" }),
    });
    const res = await PUT(req, { params: Promise.resolve({ id: "nope" }) });
    expect(res.status).toBe(404);
  });
});

describe("DELETE /api/wishes/[id]", () => {
  beforeEach(async () => {
    await fs.mkdir(DATA_DIR, { recursive: true });
    const wish: WishItem = {
      id: "wish-1",
      title: "맥북 프로",
      category: "item",
      price: null,
      url: null,
      imageUrl: null,
      memo: null,
      completed: false,
      completedAt: null,
      createdAt: "2026-04-02T00:00:00Z",
    };
    await fs.writeFile(WISH_PATH, JSON.stringify({ wishes: [wish] }));
  });

  it("deletes a wish", async () => {
    const req = new Request("http://localhost/api/wishes/wish-1", { method: "DELETE" });
    const res = await DELETE(req, { params: Promise.resolve({ id: "wish-1" }) });
    expect(res.status).toBe(200);

    const getRes = await GET();
    const body = await getRes.json();
    expect(body.data).toHaveLength(0);
  });

  it("returns 404 for unknown id", async () => {
    const req = new Request("http://localhost/api/wishes/nope", { method: "DELETE" });
    const res = await DELETE(req, { params: Promise.resolve({ id: "nope" }) });
    expect(res.status).toBe(404);
  });
});
```

파일 상단 import도 수정 필요:
```typescript
// 기존 import 라인에 추가
import { GET, POST } from "@/app/api/wishes/route";
// 아래 추가
import { PUT, DELETE } from "@/app/api/wishes/[id]/route";
import type { WishItem } from "@/types";
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npx vitest run lib/__tests__/api-wishes.test.ts`
Expected: FAIL — `[id]/route` 모듈이 존재하지 않음

- [ ] **Step 3: PUT, DELETE 라우트 구현**

`app/api/wishes/[id]/route.ts`:

```typescript
import { NextResponse } from "next/server";
import { readWishes, writeWishes } from "@/lib/wish-store";
import type { UpdateWishRequest, WishItem, ApiResponse } from "@/types";

const VALID_CATEGORIES = ["item", "experience"];

export async function PUT(
  request: Request,
  { params }: { params: Promise<{ id: string }> }
): Promise<NextResponse<ApiResponse<WishItem>>> {
  try {
    const { id } = await params;
    const body: UpdateWishRequest = await request.json();
    const wishes = await readWishes();
    const index = wishes.findIndex((w) => w.id === id);

    if (index === -1) {
      return NextResponse.json({ error: "위시를 찾을 수 없습니다." }, { status: 404 });
    }

    if (body.title !== undefined) {
      if (typeof body.title !== "string" || body.title.trim().length === 0 || body.title.length > 200) {
        return NextResponse.json({ error: "제목은 1~200자여야 합니다." }, { status: 400 });
      }
      wishes[index].title = body.title.trim();
    }
    if (body.category !== undefined) {
      if (!VALID_CATEGORIES.includes(body.category)) {
        return NextResponse.json({ error: "올바른 카테고리를 선택해주세요." }, { status: 400 });
      }
      wishes[index].category = body.category;
    }
    if (body.price !== undefined) wishes[index].price = body.price;
    if (body.url !== undefined) wishes[index].url = body.url;
    if (body.imageUrl !== undefined) wishes[index].imageUrl = body.imageUrl;
    if (body.memo !== undefined) wishes[index].memo = body.memo;
    if (body.completed !== undefined) {
      wishes[index].completed = body.completed;
      wishes[index].completedAt = body.completed ? new Date().toISOString() : null;
    }

    await writeWishes(wishes);
    return NextResponse.json({ data: wishes[index] });
  } catch {
    return NextResponse.json({ error: "서버 오류가 발생했습니다." }, { status: 500 });
  }
}

export async function DELETE(
  _request: Request,
  { params }: { params: Promise<{ id: string }> }
): Promise<NextResponse<ApiResponse<null>>> {
  try {
    const { id } = await params;
    const wishes = await readWishes();
    const index = wishes.findIndex((w) => w.id === id);

    if (index === -1) {
      return NextResponse.json({ error: "위시를 찾을 수 없습니다." }, { status: 404 });
    }

    wishes.splice(index, 1);
    await writeWishes(wishes);
    return NextResponse.json({ data: null });
  } catch {
    return NextResponse.json({ error: "서버 오류가 발생했습니다." }, { status: 500 });
  }
}
```

- [ ] **Step 4: 전체 API 테스트 통과 확인**

Run: `npx vitest run lib/__tests__/api-wishes.test.ts`
Expected: 9 tests PASS

- [ ] **Step 5: 커밋**

```bash
git add app/api/wishes/[id]/route.ts lib/__tests__/api-wishes.test.ts
git commit -m "feat(wish): add PUT/DELETE /api/wishes/[id] routes"
```

---

### Task 5: WishItem 컴포넌트

**Files:**
- Create: `components/WishItem.tsx`

- [ ] **Step 1: WishItem 카드 컴포넌트 작성**

`components/WishItem.tsx`:

```tsx
"use client";

import type { WishItem as WishItemType } from "@/types";

interface WishItemProps {
  wish: WishItemType;
  onToggle: (id: string) => void;
  onEdit: (wish: WishItemType) => void;
  onDelete: (id: string) => void;
}

function formatPrice(price: number): string {
  return price.toLocaleString("ko-KR") + "원";
}

export default function WishItem({ wish, onToggle, onEdit, onDelete }: WishItemProps) {
  return (
    <div
      className={`flex items-center gap-3.5 px-4 md:px-6 min-h-[72px] py-3 transition-opacity ${
        wish.completed ? "opacity-40" : ""
      }`}
    >
      {/* Checkbox */}
      <button
        className="w-[28px] h-[28px] rounded-full border-2 flex items-center justify-center flex-shrink-0 transition-colors"
        style={{
          borderColor: wish.completed ? "var(--accent-primary)" : "var(--label-quaternary)",
          backgroundColor: wish.completed ? "var(--accent-primary)" : "transparent",
        }}
        onClick={(e) => {
          e.stopPropagation();
          onToggle(wish.id);
        }}
      >
        {wish.completed && (
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none" stroke="white" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
            <polyline points="2.5 7 5.5 10 11.5 4" />
          </svg>
        )}
      </button>

      {/* Thumbnail or icon */}
      {wish.imageUrl ? (
        <div
          className="w-[52px] h-[52px] rounded-[10px] bg-[var(--fill-quaternary)] bg-cover bg-center flex-shrink-0"
          style={{ backgroundImage: `url(${wish.imageUrl})` }}
        />
      ) : (
        <div className="w-[52px] h-[52px] rounded-[10px] bg-[var(--fill-quaternary)] flex items-center justify-center flex-shrink-0">
          <span className="text-[24px]">
            {wish.category === "item" ? "🛍️" : "⭐"}
          </span>
        </div>
      )}

      {/* Content — clickable for edit */}
      <button
        className="flex-1 min-w-0 text-left"
        onClick={() => onEdit(wish)}
      >
        <div className={`text-[20px] leading-[26px] font-semibold truncate ${
          wish.completed ? "line-through text-[var(--label-tertiary)]" : "text-[var(--label-primary)]"
        }`}>
          {wish.title}
        </div>
        <div className="flex items-center gap-1.5 mt-0.5">
          {wish.price !== null && (
            <span className="text-[15px] text-[var(--accent-primary)] font-medium">
              {formatPrice(wish.price)}
            </span>
          )}
          {wish.memo && (
            <span className="text-[15px] text-[var(--label-tertiary)] truncate">
              {wish.price !== null && <span className="text-[var(--label-quaternary)]"> · </span>}
              {wish.memo}
            </span>
          )}
        </div>
      </button>

      {/* Delete button */}
      <button
        className="w-[44px] h-[44px] flex items-center justify-center flex-shrink-0 text-[var(--label-quaternary)]"
        onClick={(e) => {
          e.stopPropagation();
          onDelete(wish.id);
        }}
      >
        <svg width="18" height="18" viewBox="0 0 18 18" fill="none" stroke="currentColor" strokeWidth="1.6" strokeLinecap="round">
          <line x1="4.5" y1="4.5" x2="13.5" y2="13.5" />
          <line x1="13.5" y1="4.5" x2="4.5" y2="13.5" />
        </svg>
      </button>
    </div>
  );
}
```

- [ ] **Step 2: 커밋**

```bash
git add components/WishItem.tsx
git commit -m "feat(wish): add WishItem card component"
```

---

### Task 6: AddWishSheet 컴포넌트

**Files:**
- Create: `components/AddWishSheet.tsx`

- [ ] **Step 1: AddWishSheet 바텀 시트 작성**

`components/AddWishSheet.tsx`:

```tsx
"use client";

import { useState } from "react";
import type { WishItem, WishCategory } from "@/types";

interface AddWishSheetProps {
  wish?: WishItem | null;
  defaultCategory: WishCategory;
  onSave: (data: {
    title: string;
    category: WishCategory;
    price: number | null;
    url: string | null;
    imageUrl: string | null;
    memo: string | null;
  }) => void;
  onDelete?: (id: string) => void;
  onClose: () => void;
}

export default function AddWishSheet({
  wish,
  defaultCategory,
  onSave,
  onDelete,
  onClose,
}: AddWishSheetProps) {
  const [title, setTitle] = useState(wish?.title ?? "");
  const [category, setCategory] = useState<WishCategory>(wish?.category ?? defaultCategory);
  const [priceText, setPriceText] = useState(wish?.price != null ? String(wish.price) : "");
  const [url, setUrl] = useState(wish?.url ?? "");
  const [imageUrl, setImageUrl] = useState(wish?.imageUrl ?? "");
  const [memo, setMemo] = useState(wish?.memo ?? "");

  const handleSave = () => {
    if (!title.trim()) return;
    const price = priceText.trim() ? Number(priceText.replace(/,/g, "")) : null;
    onSave({
      title: title.trim(),
      category,
      price: price !== null && !isNaN(price) ? price : null,
      url: url.trim() || null,
      imageUrl: imageUrl.trim() || null,
      memo: memo.trim() || null,
    });
  };

  const canSave = title.trim().length > 0;

  return (
    <div className="fixed inset-0 z-50 flex items-end justify-center bg-black/40" onClick={onClose}>
      <div
        className="w-full max-w-lg bg-[var(--bg-primary)] rounded-t-3xl p-6 animate-[sheetUp_0.3s_ease-out] max-h-[85dvh] overflow-y-auto"
        onClick={(e) => e.stopPropagation()}
      >
        <div className="w-10 h-1 bg-[var(--fill-tertiary)] rounded-full mx-auto mb-6" />

        <div className="flex items-center justify-between mb-6">
          <button className="text-[var(--label-tertiary)] text-[20px]" onClick={onClose}>
            취소
          </button>
          <h2 className="text-[20px] font-bold text-[var(--label-primary)]">
            {wish ? "위시 수정" : "새 위시"}
          </h2>
          <button
            className={`text-[20px] font-bold ${canSave ? "text-[var(--accent-primary)]" : "text-[var(--label-quaternary)]"}`}
            onClick={handleSave}
            disabled={!canSave}
          >
            저장
          </button>
        </div>

        {/* 제목 */}
        <input
          type="text"
          value={title}
          onChange={(e) => setTitle(e.target.value)}
          placeholder="위시 이름"
          maxLength={200}
          className="w-full px-4 py-3.5 rounded-xl bg-[var(--fill-quaternary)] text-[20px] text-[var(--label-primary)] placeholder:text-[var(--label-quaternary)] outline-none mb-4"
        />

        {/* 카테고리 */}
        <div className="flex gap-2 mb-4">
          {(["item", "experience"] as const).map((c) => (
            <button
              key={c}
              className={`flex-1 py-3 rounded-xl text-[17px] font-medium transition-colors ${
                category === c
                  ? "bg-[var(--accent-primary)] text-white"
                  : "bg-[var(--fill-quaternary)] text-[var(--label-secondary)]"
              }`}
              onClick={() => setCategory(c)}
            >
              {c === "item" ? "🛍️ 물건" : "⭐ 경험"}
            </button>
          ))}
        </div>

        {/* 가격 */}
        <input
          type="text"
          inputMode="numeric"
          value={priceText}
          onChange={(e) => setPriceText(e.target.value.replace(/[^0-9]/g, ""))}
          placeholder="예상 가격 (원)"
          className="w-full px-4 py-3.5 rounded-xl bg-[var(--fill-quaternary)] text-[20px] text-[var(--label-primary)] placeholder:text-[var(--label-quaternary)] outline-none mb-4"
        />

        {/* 링크 */}
        <input
          type="url"
          value={url}
          onChange={(e) => setUrl(e.target.value)}
          placeholder="링크 (선택)"
          className="w-full px-4 py-3.5 rounded-xl bg-[var(--fill-quaternary)] text-[20px] text-[var(--label-primary)] placeholder:text-[var(--label-quaternary)] outline-none mb-4"
        />

        {/* 이미지 URL */}
        <input
          type="url"
          value={imageUrl}
          onChange={(e) => setImageUrl(e.target.value)}
          placeholder="이미지 URL (선택)"
          className="w-full px-4 py-3.5 rounded-xl bg-[var(--fill-quaternary)] text-[20px] text-[var(--label-primary)] placeholder:text-[var(--label-quaternary)] outline-none mb-4"
        />

        {/* 메모 */}
        <textarea
          value={memo}
          onChange={(e) => setMemo(e.target.value)}
          placeholder="메모 (선택)"
          rows={3}
          className="w-full px-4 py-3.5 rounded-xl bg-[var(--fill-quaternary)] text-[20px] text-[var(--label-primary)] placeholder:text-[var(--label-quaternary)] outline-none mb-4 resize-none"
        />

        {/* 삭제 버튼 (수정 모드) */}
        {wish && onDelete && (
          <button
            className="w-full py-3.5 mt-2 rounded-xl text-[20px] font-medium text-[var(--system-red)] bg-[var(--system-red)]/10"
            onClick={() => onDelete(wish.id)}
          >
            위시 삭제
          </button>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 커밋**

```bash
git add components/AddWishSheet.tsx
git commit -m "feat(wish): add AddWishSheet bottom sheet component"
```

---

### Task 7: WishlistView 컴포넌트

**Files:**
- Create: `components/WishlistView.tsx`

- [ ] **Step 1: WishlistView 작성**

`components/WishlistView.tsx`:

```tsx
"use client";

import type { WishItem as WishItemType, WishCategory } from "@/types";
import SectionTabs from "@/components/SectionTabs";
import WishItem from "@/components/WishItem";

interface WishlistViewProps {
  wishes: WishItemType[];
  wishTab: WishCategory;
  onTabChange: (tab: WishCategory) => void;
  onToggle: (id: string) => void;
  onEdit: (wish: WishItemType) => void;
  onDelete: (id: string) => void;
  onAdd: () => void;
}

export default function WishlistView({
  wishes,
  wishTab,
  onTabChange,
  onToggle,
  onEdit,
  onDelete,
  onAdd,
}: WishlistViewProps) {
  const filtered = wishes.filter((w) => w.category === wishTab);
  const active = filtered.filter((w) => !w.completed);
  const completed = filtered.filter((w) => w.completed);
  const itemCount = wishes.filter((w) => w.category === "item" && !w.completed).length;
  const expCount = wishes.filter((w) => w.category === "experience" && !w.completed).length;

  return (
    <>
      <div className="md:px-8 pt-1 pb-2">
        <SectionTabs
          tabs={[
            { key: "item", label: `물건${itemCount > 0 ? ` ${itemCount}` : ""}` },
            { key: "experience", label: `경험${expCount > 0 ? ` ${expCount}` : ""}` },
          ]}
          active={wishTab}
          onChange={(key) => onTabChange(key as WishCategory)}
        />
      </div>

      <main className="flex-1 overflow-y-auto pb-20 md:pb-8">
        <div className="md:px-8">
          {active.length === 0 && completed.length === 0 ? (
            <div className="flex flex-col items-center justify-center py-24 text-[var(--label-tertiary)]">
              <span className="text-[56px] mb-5 opacity-30">
                {wishTab === "item" ? "🛍️" : "⭐"}
              </span>
              <p className="text-[20px]">
                {wishTab === "item" ? "갖고 싶은 물건을 추가해보세요" : "하고 싶은 경험을 추가해보세요"}
              </p>
            </div>
          ) : (
            <>
              {active.length > 0 && (
                <div className="mx-5 md:mx-0 flex flex-col gap-2.5">
                  {active.map((wish) => (
                    <div key={wish.id} className="bg-[var(--sys-bg-elevated)] rounded-xl overflow-hidden">
                      <WishItem wish={wish} onToggle={onToggle} onEdit={onEdit} onDelete={onDelete} />
                    </div>
                  ))}
                </div>
              )}
              {completed.length > 0 && (
                <div className="mx-5 md:mx-0 mt-6">
                  <p className="text-[15px] font-medium text-[var(--label-tertiary)] mb-2 px-1">
                    완료 {completed.length}
                  </p>
                  <div className="flex flex-col gap-2.5">
                    {completed.map((wish) => (
                      <div key={wish.id} className="bg-[var(--sys-bg-elevated)] rounded-xl overflow-hidden">
                        <WishItem wish={wish} onToggle={onToggle} onEdit={onEdit} onDelete={onDelete} />
                      </div>
                    ))}
                  </div>
                </div>
              )}
            </>
          )}

          <div className="mx-5 md:mx-0 mt-4">
            <button
              className="w-full py-3.5 rounded-xl bg-[var(--accent-primary)] text-white text-[20px] font-semibold active:opacity-80 transition-opacity"
              onClick={onAdd}
            >
              새 위시 추가
            </button>
          </div>
        </div>
      </main>
    </>
  );
}
```

- [ ] **Step 2: 커밋**

```bash
git add components/WishlistView.tsx
git commit -m "feat(wish): add WishlistView with tab filtering"
```

---

### Task 8: 네비게이션 및 메인 페이지 연결

**Files:**
- Modify: `components/BottomNav.tsx`
- Modify: `app/page.tsx`

- [ ] **Step 1: BottomNav에 위시리스트 탭 추가**

`components/BottomNav.tsx` 수정 — 탭 순서를 **할일 / 노트 / 위시리스트 / D-day**로 변경. 노트와 D-day 버튼 사이에 위시리스트 버튼을 삽입한다:

노트 버튼과 D-day 버튼 사이에 다음 버튼을 추가:

```tsx
{/* 위시리스트 */}
<button
  className={`flex-1 flex flex-col items-center justify-center gap-0.5 transition-colors ${
    active === "wish"
      ? "text-[var(--accent-primary)]"
      : "text-[var(--label-tertiary)]"
  }`}
  onClick={() => onChange("wish")}
>
  <svg width="25" height="25" viewBox="0 0 25 25" fill={active === "wish" ? "currentColor" : "none"} stroke="currentColor" strokeWidth={active === "wish" ? "0" : "1.8"} strokeLinecap="round" strokeLinejoin="round">
    {active === "wish" ? (
      <path d="M12.5 4.5l2.3 4.7 5.2.8-3.7 3.7.9 5.1-4.7-2.5-4.7 2.5.9-5.1-3.7-3.7 5.2-.8z" />
    ) : (
      <path d="M12.5 4.5l2.3 4.7 5.2.8-3.7 3.7.9 5.1-4.7-2.5-4.7 2.5.9-5.1-3.7-3.7 5.2-.8z" />
    )}
  </svg>
  <span className="text-[12px] font-medium">위시</span>
</button>
```

- [ ] **Step 2: page.tsx에 위시리스트 state와 로직 추가**

`app/page.tsx` 수정. 컴포넌트 최상위에 state 추가:

```typescript
import WishlistView from "@/components/WishlistView";
import AddWishSheet from "@/components/AddWishSheet";
import type { Todo, Schedule, WishItem, ScheduleType, RepeatMode, WishCategory, Section, NoteTab } from "@/types";

// Home 컴포넌트 내부, 기존 state 선언부에 추가:
const [wishes, setWishes] = useState<WishItem[]>([]);
const [wishTab, setWishTab] = useState<WishCategory>("item");
const [showAddWish, setShowAddWish] = useState(false);
const [editWish, setEditWish] = useState<WishItem | null>(null);
```

fetchWishes 함수 추가:

```typescript
const fetchWishes = useCallback(async () => {
  try {
    const res = await fetch(`${BASE}/api/wishes`);
    const body = await res.json();
    if (body.data) setWishes(body.data);
  } catch (err) {
    console.error("Failed to fetch wishes:", err);
  }
}, []);
```

useEffect에 fetchWishes 추가:

```typescript
useEffect(() => {
  Promise.all([fetchTodos(), fetchSchedules(), fetchWishes()]).finally(() =>
    setLoading(false)
  );
}, [fetchTodos, fetchSchedules, fetchWishes]);
```

위시 CRUD 함수 추가:

```typescript
// Wish actions
const saveWish = async (data: {
  title: string;
  category: WishCategory;
  price: number | null;
  url: string | null;
  imageUrl: string | null;
  memo: string | null;
}) => {
  try {
    if (editWish) {
      const res = await fetch(`${BASE}/api/wishes/${editWish.id}`, {
        method: "PUT",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(data),
      });
      const body = await res.json();
      if (body.data) {
        setWishes((prev) => prev.map((w) => (w.id === editWish.id ? body.data : w)));
      }
    } else {
      const res = await fetch(`${BASE}/api/wishes`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(data),
      });
      const body = await res.json();
      if (body.data) setWishes((prev) => [...prev, body.data]);
    }
  } catch (err) {
    console.error("Failed to save wish:", err);
  }
  setShowAddWish(false);
  setEditWish(null);
};

const toggleWish = async (id: string) => {
  const wish = wishes.find((w) => w.id === id);
  if (!wish) return;
  const res = await fetch(`${BASE}/api/wishes/${id}`, {
    method: "PUT",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ completed: !wish.completed }),
  });
  const body = await res.json();
  if (body.data) {
    setWishes((prev) => prev.map((w) => (w.id === id ? body.data : w)));
  }
};

const deleteWish = async (id: string) => {
  setWishes((prev) => prev.filter((w) => w.id !== id));
  await fetch(`${BASE}/api/wishes/${id}`, { method: "DELETE" });
  setShowAddWish(false);
  setEditWish(null);
};
```

- [ ] **Step 3: Content 영역에 위시리스트 뷰 연결**

`page.tsx`의 Content 내부를 수정한다.

모바일 헤더의 제목 부분:
```typescript
{section === "todo" ? "할 일" : section === "note" ? "노트" : section === "wish" ? "위시리스트" : "D-day"}
```

Tabs 영역에 위시리스트 분기 추가 (기존 `section === "note"` 분기 뒤에):
```tsx
) : section === "wish" ? (
  null  // WishlistView가 자체적으로 탭을 렌더링
```

Main content 영역에 위시리스트 분기 추가 (note 분기 뒤에):
```tsx
) : section === "wish" ? (
  <WishlistView
    wishes={wishes}
    wishTab={wishTab}
    onTabChange={setWishTab}
    onToggle={toggleWish}
    onEdit={(w) => {
      setEditWish(w);
      setShowAddWish(true);
    }}
    onDelete={deleteWish}
    onAdd={() => {
      setEditWish(null);
      setShowAddWish(true);
    }}
  />
```

위시리스트 뷰는 자체적으로 SectionTabs와 main 스크롤 영역을 포함하므로, page.tsx의 Tabs 영역에서는 `null`을 반환하고, Main 영역에서 `WishlistView`를 렌더링한다.

- [ ] **Step 4: handleSwipe에 위시 분기 추가**

```typescript
const handleSwipe = (dir: "left" | "right") => {
  if (section === "todo") {
    if (dir === "left" && todoTab === "now") setTodoTab("soon");
    if (dir === "right" && todoTab === "soon") setTodoTab("now");
  } else if (section === "note") {
    if (dir === "left" && noteTab === "daily") setNoteTab("general");
    if (dir === "right" && noteTab === "general") setNoteTab("daily");
  } else if (section === "wish") {
    if (dir === "left" && wishTab === "item") setWishTab("experience");
    if (dir === "right" && wishTab === "experience") setWishTab("item");
  } else {
    if (dir === "left" && ddayTab === "general") setDdayTab("anniversary");
    if (dir === "right" && ddayTab === "anniversary") setDdayTab("general");
  }
};
```

- [ ] **Step 5: 사이드바에 위시리스트 메뉴 추가**

Sidebar의 `nav` 안에서 노트 버튼과 D-day 버튼 사이에 위시리스트 버튼을 추가한다:

```tsx
<button
  className={`w-full flex items-center gap-3 px-3 py-2.5 rounded-xl text-left transition-colors ${
    section === "wish"
      ? "bg-[var(--accent-primary)]/12 text-[var(--accent-primary)]"
      : "text-[var(--label-primary)] hover:bg-[var(--fill-quaternary)]"
  }`}
  onClick={() => setSection("wish")}
>
  <svg width="22" height="22" viewBox="0 0 22 22" fill={section === "wish" ? "currentColor" : "none"} stroke="currentColor" strokeWidth={section === "wish" ? "0" : "1.6"} strokeLinecap="round" strokeLinejoin="round">
    {section === "wish" ? (
      <path d="M11 2l2.5 5.1 5.6.8-4.1 4 .9 5.5L11 14.8l-5 2.6.9-5.5-4.1-4 5.6-.8z" />
    ) : (
      <path d="M11 2l2.5 5.1 5.6.8-4.1 4 .9 5.5L11 14.8l-5 2.6.9-5.5-4.1-4 5.6-.8z" />
    )}
  </svg>
  <span className="text-[15px] font-medium flex-1">위시리스트</span>
  {wishes.filter((w) => !w.completed).length > 0 && (
    <span className="text-[13px] text-[var(--label-tertiary)]">
      {wishes.filter((w) => !w.completed).length}
    </span>
  )}
</button>
```

- [ ] **Step 6: AddWishSheet 모달 렌더링 추가**

return문의 모달 영역에 추가 (AddScheduleSheet 아래):

```tsx
{showAddWish && (
  <AddWishSheet
    wish={editWish}
    defaultCategory={wishTab}
    onSave={saveWish}
    onDelete={editWish ? deleteWish : undefined}
    onClose={() => {
      setShowAddWish(false);
      setEditWish(null);
    }}
  />
)}
```

- [ ] **Step 7: 커밋**

```bash
git add components/BottomNav.tsx app/page.tsx
git commit -m "feat(wish): integrate wishlist into navigation and main page"
```

---

### Task 9: 빌드 & 배포

**Files:** (없음 — 빌드/배포 커맨드만)

- [ ] **Step 1: 전체 테스트 통과 확인**

Run: `npx vitest run`
Expected: 모든 테스트 PASS

- [ ] **Step 2: 빌드 확인**

```bash
pm2 stop v-todo
rm -rf .next
npx next build
```

Expected: 빌드 성공, `BUILD_ID`와 `prerender-manifest.json` 존재 확인

- [ ] **Step 3: 배포**

```bash
pm2 start v-todo
pm2 save
```

- [ ] **Step 4: 브라우저에서 위시리스트 탭 확인**

접속 후 확인 사항:
- 하단 탭에 "위시" 탭이 노트와 D-day 사이에 보이는지
- 물건/경험 내부 탭이 동작하는지
- 새 위시 추가/수정/삭제/완료 토글이 동작하는지
- 데스크톱 사이드바에도 "위시리스트" 메뉴가 보이는지

- [ ] **Step 5: 최종 커밋 (필요 시)**

```bash
git add -A
git commit -m "chore: build and deploy wishlist feature"
```
